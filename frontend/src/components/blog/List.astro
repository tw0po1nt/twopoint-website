---
import Item from '~/components/blog/ListItem.astro';
import type { Post } from '~/types';

export interface Props {
  posts: Array<Post>;
  seriesSlug?: string;
}

const { posts, seriesSlug } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} seriesSlug={seriesSlug} />
      </li>
    ))
  }
</ul>
